<div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_path 'tools.jpg' %>);">
  <div class="container">
    <h1>The tools you need with <strong>Toolshed</strong></h1>
    <p><span id="banner-typed-text"></span></p>
    <span class="col-8" id="banner-typed-text"></span>
    <!-- form needs to be edited once we have controllers -->

    <%= simple_form_for :search, url: tools_path, method: :get do |f| %>
    <div class="search-form-control form-group">
    <input class="form-control string required" type="text" name="search[query]" id="search_query" />
    <button name="button" type="submit" class="btn btn-flat">
      <i class="fas fa-search"></i> Search
    </button>
    </div>
    <% end %>
    </div>
    </div>
    <br>
    <div class="container">
    <!-- add category cards view grid, eg 6 categories. -->
    <h5>Browse Tools</h5>
    <%= render "shared/card_grid" %>
    <!-- add index cards view grid, eg first 20 of all listings avail. -->
    </div>
